# Environment Variables Example
# Copy this file to .env.development, .env.staging, or .env.production
# and fill in the values

# Backend API Configuration
# Production: https://job-finder-api.joshwentworth.com
# Development: http://localhost:8080
VITE_API_BASE_URL=https://job-finder-api.joshwentworth.com

# Build metadata
# Values: "development" | "staging" | "production"
# In development mode, auth modal shows role buttons (Public/Viewer/Admin)
# instead of Google OAuth for rapid local testing
VITE_ENVIRONMENT=production

# Google OAuth Configuration
# Required for staging/production authentication via Google Identity Services (GIS)
# Not required in development mode - the app uses mock dev tokens instead
VITE_GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id

# Analytics
VITE_ENABLE_ANALYTICS=false

# Firebase Project ID (used for Cloud Logging only)
VITE_FIREBASE_PROJECT_ID=static-sites-257923
