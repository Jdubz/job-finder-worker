/**
 * Re-export date formatting utilities from the centralized module.
 *
 * @deprecated Import directly from "@/utils/dateFormat" instead.
 * This file exists for backward compatibility.
 */
export { formatDate, formatDateTime, normalizeDateValue } from "@/utils/dateFormat"
