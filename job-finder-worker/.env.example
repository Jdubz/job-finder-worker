# Environment Variables - Flask Application
# Copy this file to .env and fill in your actual values

# ==============================================================================
# AI Provider API Keys (OPTIONAL - depends on interface type)
# ==============================================================================
# API keys are ONLY needed when using "api" interface providers.
# CLI-based providers (codex/cli, gemini/cli) use OAuth credentials instead.
#
# Production uses CLI interfaces with mounted OAuth credentials:
# - Codex CLI: ~/.codex/auth.json (via `codex login`)
# - Gemini CLI: ~/.gemini/oauth_creds.json (via `gemini auth login`)
#
# Only set these if you're using API interfaces in ai-settings config:
# - Anthropic Claude: https://console.anthropic.com/
# - OpenAI: https://platform.openai.com/api-keys
# - Google Gemini: https://aistudio.google.com/app/apikey
# ==============================================================================

# Anthropic Claude API Key (only for claude/api interface)
ANTHROPIC_API_KEY=

# OpenAI API Key (only for openai/api interface)
OPENAI_API_KEY=

# Google Gemini API Key (only for gemini/api interface)
# Not needed if using gemini/cli with OAuth credentials
GOOGLE_API_KEY=

# ==============================================================================
# Firebase credentials (legacy; not used by SQLite worker)
# ==============================================================================
# Path to your Firebase service account JSON file
# Download from: Firebase Console > Project Settings > Service Accounts
# NOTE: Not needed for local development with SQLite
# ==============================================================================

# Path to Firebase service account credentials JSON file (OPTIONAL)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/firebase-key.json
# Example: /home/user/.firebase/job-finder-key.json

# ==============================================================================
# Flask Worker Configuration
# ==============================================================================
# Configuration for the Flask worker application
# ==============================================================================

# Worker HTTP server settings
WORKER_PORT=5555
WORKER_HOST=127.0.0.1  # Use 0.0.0.0 to allow external connections
FLASK_ENV=development  # or 'production'

# Queue polling configuration
POLL_INTERVAL=60  # Seconds between queue polls (default: 60)

# Logging configuration
QUEUE_WORKER_LOG_FILE=logs/worker.log
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL

# Database name overrides (overrides config file values)
# For development/staging:
PROFILE_DATABASE_NAME=job-finder-staging
STORAGE_DATABASE_NAME=job-finder-staging

# For production:
# PROFILE_DATABASE_NAME=job-finder
# STORAGE_DATABASE_NAME=job-finder

# ==============================================================================
# Optional: Additional Configuration
# ==============================================================================

# Python path (usually set automatically by run scripts)
# PYTHONPATH=./src

# ==============================================================================
# Search API Configuration
# ==============================================================================
# Used for company discovery to provide search results to AI agent
# Choose one provider (Tavily recommended)
# ==============================================================================

# Tavily Search API (recommended, LLM-optimized)
# Get API key from: https://tavily.com/
# Free tier: 1,000 requests/month
TAVILY_API_KEY=

# Brave Search API (alternative)
# Get API key from: https://brave.com/search/api/
# Free tier: 2,000 requests/month
BRAVE_API_KEY=

# ==============================================================================
# Optional: Job Board API Keys
# ==============================================================================
# Only needed if you enable these sources in job-listings
# ==============================================================================

# Adzuna API (free tier available)
# Get keys from: https://developer.adzuna.com/
ADZUNA_APP_ID=
ADZUNA_API_KEY=

# ==============================================================================
# DEPRECATED: Docker-specific variables (no longer used)
# ==============================================================================
# These variables were used for Docker deployment and are no longer needed
# Keep them for reference but they won't be used by the Flask application
# ==============================================================================

# ENVIRONMENT=staging
# TZ=America/Los_Angeles
# LOG_FILE=/app/logs/scheduler.log

# ==============================================================================
# Notes
# ==============================================================================
# 1. Never commit .env file to version control
# 2. Use absolute paths for credential files
# 3. Ensure proper file permissions (chmod 600 .env)
# 4. See LOCAL_DEVELOPMENT.md for setup instructions
# 5. See FLASK_DEPLOYMENT.md for deployment instructions
# ==============================================================================
