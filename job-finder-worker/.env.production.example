# Job Finder - Production Environment Variables
# Copy this to .env.production and fill in your actual values
# These should be set in Portainer Stack Environment Variables

# ============================================================================
# API KEYS (REQUIRED)
# ============================================================================
# Anthropic API Key for Claude AI
# Get from: https://console.anthropic.com/
# IMPORTANT: Use production-tier key with higher rate limits
ANTHROPIC_API_KEY=sk-ant-api03-...your-production-key...

# OpenAI API Key (optional, if using GPT models)
# Get from: https://platform.openai.com/api-keys
# IMPORTANT: Use production-tier key with higher rate limits
OPENAI_API_KEY=sk-...your-production-key...

# ============================================================================
# FIREBASE CREDENTIALS
# ============================================================================
# Path to Firebase service account JSON (mounted as volume)
# This is set in docker-compose.production.yml - DO NOT override
GOOGLE_APPLICATION_CREDENTIALS=/app/credentials/serviceAccountKey.json

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# These are set in docker-compose.production.yml - DO NOT override unless needed
# PROFILE_DATABASE_NAME=portfolio
# STORAGE_DATABASE_NAME=portfolio

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================
# Environment identifier
# This is set in docker-compose.production.yml - DO NOT override
# ENVIRONMENT=production

# Config file path (uses production-specific config)
# This is set in docker-compose.production.yml - DO NOT override
# CONFIG_PATH=/app/config/config.production.yaml

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================
# Log file paths (mounted volumes)
# These are set in docker-compose.production.yml - DO NOT override
# LOG_FILE=/app/logs/scheduler.log
# QUEUE_WORKER_LOG_FILE=/app/logs/queue_worker.log

# ============================================================================
# NOTIFICATIONS (OPTIONAL)
# ============================================================================
# Watchtower update notifications
# Discord webhook format: discord://webhook_token@webhook_id
# Slack webhook format: slack://token@channel
# RECOMMENDED: Set this for production to get notified of deployments
WATCHTOWER_NOTIFICATION_URL=

# ============================================================================
# TIMEZONE
# ============================================================================
# Timezone for cron scheduling
# This is set in docker-compose.production.yml - DO NOT override
# TZ=America/Los_Angeles

# ============================================================================
# PRODUCTION SAFETY NOTES
# ============================================================================
# - ALWAYS use production-tier API keys with higher rate limits
# - Database names MUST be "portfolio" (no "-staging" suffix)
# - Never test experimental features directly in production
# - Always test in staging first before deploying to production
# - Set up Watchtower notifications to monitor deployments
# - Monitor resource usage and adjust limits if needed
# - Set up log rotation to prevent disk space issues
# - Keep separate credentials from staging environment
