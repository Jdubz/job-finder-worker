# Copy to .env.dev (gitignored) before running docker dev environment.
#
# CODEX CREDENTIALS:
# - Bind-mounted from ~/.codex to /home/node/.codex in the container
# - Container runs as node user (uid 1000) to prevent host session file lockout
# - Must be read-write for OAuth token refresh
#
# USAGE:
#   cp .env.dev.example .env.dev
#   make dev-setup
#   make dev-clone-db PROD_DB=/path/to/jobfinder.db
#   make dev-up

API_PORT=18080

# Token for generator API bypass authentication
GENERATOR_BYPASS_TOKEN=dev-token-change-me

# Optional overrides
# GENERATOR_ARTIFACTS_PUBLIC_BASE=http://localhost:18080/api/generator/artifacts
