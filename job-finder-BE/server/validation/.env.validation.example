# Copy to .env.validation (kept gitignored) before running the validation harness.
# Keep credential mounts identical to production by pointing CODEX_DIR at the
# same directory you mount in prod (typically ~/.codex copied from the server).

API_PORT=18080
# Use the exact path you mount in production (do not copy credentials into git)
CODEX_DIR=/home/you/.codex
# Token used by run-validation.sh and curl requests. Should match TEST_AUTH_BYPASS_TOKEN
TEST_AUTH_BYPASS_TOKEN=validation-bypass-token
GENERATOR_BYPASS_TOKEN=validation-bypass-token

# Optional overrides
# GENERATOR_ARTIFACTS_PUBLIC_BASE=http://localhost:18080/api/generator/artifacts
# PROD_SSH_HOST=prod-jobfinder            # used by clone-prod-db.sh
# PROD_DB_PATH=/srv/job-finder/data/jobfinder.db
