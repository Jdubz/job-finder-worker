# Example env vars for the Node API.
# Copy to .env.local for local development only. Production values live in ../.env.

# Core runtime
# When NODE_ENV=development, the API accepts dev tokens (dev-admin-token, dev-viewer-token)
# for authentication without Google OAuth
NODE_ENV=development
PORT=8080
DATABASE_PATH=/absolute/path/to/jobfinder.db

# Auth / security
# Google OAuth is not required in development mode - dev tokens are accepted instead
GOOGLE_OAUTH_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
TEST_AUTH_BYPASS_TOKEN=local-test-bypass-token
FIREBASE_APP_CHECK_AUDIENCE=legacy-app-check-token  # Optional, only if FE still sends it during transition

# Generator + AI adapters
GENERATOR_FUNCTION_URL=https://generator-service.example.com
GENERATOR_ARTIFACTS_DIR=/tmp/job-finder/artifacts
GENERATOR_ARTIFACTS_PUBLIC_BASE=https://cdn.example.com/artifacts
GOOGLE_API_KEY=your-google-api-key

# Optional worker helpers (used via shared .env)
ANTHROPIC_API_KEY=your-anthropic-key
OPENAI_API_KEY=your-openai-key

# Notes:
# - Admin authorization is sourced entirely from the SQLite users.roles column.
#   Do not define admin allowlists in .env.
# - Firebase service accounts are no longer referenced by the API; only the
#   frontend hosting workflow (job-finder-FE/.firebase/serviceAccountKey.json)
#   needs that credential, and it stays gitignored.
