# Example environment variables for job-finder-BE
# Copy this file to .env.local and fill in the values for local development
#
# IMPORTANT: In production, secrets are stored in Google Cloud Secret Manager
# The following secrets are available in static-sites-257923 project:
# - openai-api-key
# - gemini-api-key
# - mailgun-api-key
# - mailgun-domain
# - from-email
# - to-email
# - reply-to-email

# Firebase Configuration
FIREBASE_PROJECT_ID=static-sites-257923
# For local development, use Firebase emulator or service account
GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account-key.json

# Google Cloud Configuration
GCP_PROJECT_ID=static-sites-257923
GCP_REGION=us-central1
DATABASE_ID=portfolio  # Use 'portfolio' for production, 'portfolio-staging' for staging

# API Configuration
API_BASE_URL=http://localhost:5001
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000,http://localhost:5000

# AI Provider Configuration (local development only)
# In production, these are fetched from Secret Manager
OPENAI_API_KEY=your-openai-api-key-here
GEMINI_API_KEY=your-gemini-api-key-here

# Email Configuration (local development only)
# In production, these are fetched from Secret Manager
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_DOMAIN=your-mailgun-domain
FROM_EMAIL=noreply@yourdomain.com
TO_EMAIL=admin@yourdomain.com
REPLY_TO_EMAIL=noreply@yourdomain.com

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=20   # Max requests per window

# Logging
LOG_LEVEL=info  # debug, info, warning, error

# Environment
NODE_ENV=development  # development, staging, production

# Job Queue Configuration
JOB_QUEUE_COLLECTION=job-queue
JOB_MATCHES_COLLECTION=job-matches
CONFIG_COLLECTION=job-finder-config

# Storage Configuration
STORAGE_BUCKET=static-sites-257923.appspot.com
STORAGE_BUCKET_STAGING=static-sites-257923-staging.appspot.com

# Security
ALLOWED_ORIGINS=http://localhost:5173,https://job-finder.joshwentworth.com
